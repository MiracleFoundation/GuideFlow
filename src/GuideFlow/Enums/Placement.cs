namespace GuideFlow.Enums;

/// <summary>
/// Determines where a step popover is positioned relative to its target element.
/// </summary>
public enum Placement
{
    Top,
    TopStart,
    TopEnd,
    Bottom,
    BottomStart,
    BottomEnd,
    Left,
    LeftStart,
    LeftEnd,
    Right,
    RightStart,
    RightEnd,
    Auto
}
